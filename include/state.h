#ifndef STATE_H
#define STATE_H

typedef enum 
{
    STATE_SELECT_PLAYER,
    STATE_MENU,
    STATE_CATEGORY,
    STATE_INTRO,
    STATE_PLAYING,
    STATE_RESULT,
    STATE_SCOREBOARD,
    STATE_HELP,
    STATE_EXIT
} GameState;

#endif
